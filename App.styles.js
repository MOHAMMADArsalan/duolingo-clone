import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    container: {
        paddingTop: 40,
        flex: 1,
        padding:10,
        backgroundColor: '#fff',
    },
   
});

export default styles;